import 'package:flutter/material.dart';
import 'game.dart';


class RoletaPage extends StatelessWidget {
  const RoletaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Game(); // ou GameRoleta() se renomeou
  }
}
